def count_in_list(lst, element):
    """
    Count how many times 'element' appears in 'lst'.

    Args:
        lst (list): The list to search in.
        element: The element to count.

    Returns:
        int: Number of occurrences of element in lst.
    """
    return lst.count(element)
